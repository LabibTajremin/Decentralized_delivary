# P15 — Admin & auto-tuning

Depends on: P03, P08, P12
Branch: `phase/15-admin-auto-tuning`

## Tasks

```
P15.T01  <written when this phase starts — see docs/build/phases.md for the phase output>
```

> Task breakdown is authored at the start of the phase, not up front, so it
> reflects what the preceding phases actually produced.

## Acceptance

- Every Appendix B variable is admin-controllable per area
- ALG-09 adjusts only within admin bounds and logs its reasoning
- discovery.division_ceiling cannot be disabled by admin or tuner
