# P17 — Flutter foundation

Depends on: P00
Branch: `phase/17-flutter-foundation`

## Tasks

```
P17.T01  <written when this phase starts — see docs/build/phases.md for the phase output>
```

> Task breakdown is authored at the start of the phase, not up front, so it
> reflects what the preceding phases actually produced.

## Acceptance

- Design tokens come from the Figma file
- The thin-client lint is wired before any screen is built
- 48dp targets and contrast are enforced
- Bengali localization renders at Bengali string lengths
