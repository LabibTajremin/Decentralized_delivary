# P12 — Dispatch

Depends on: P11, P02, P03
Branch: `phase/12-dispatch`

## Tasks

```
P12.T01  <written when this phase starts — see docs/build/phases.md for the phase output>
```

> Task breakdown is authored at the start of the phase, not up front, so it
> reflects what the preceding phases actually produced.

## Acceptance

- A partner sees only jobs inside their radius
- Long and short distance choice per D4 is honoured
- Two partners cannot be assigned the same order
