# P03 — Config module

Depends on: P00, P01
Branch: `phase/03-config-module`

## Tasks

```
P03.T01  <written when this phase starts — see docs/build/phases.md for the phase output>
```

> Task breakdown is authored at the start of the phase, not up front, so it
> reflects what the preceding phases actually produced.

## Acceptance

- Resolution order area → district → division → global is proven by test
- Every Appendix B key has a default
- Every change is written to the audit log
