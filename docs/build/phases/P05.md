# P05 — User profile & addresses

Depends on: P04, P02
Branch: `phase/05-user-profile-addresses`

## Tasks

```
P05.T01  <written when this phase starts — see docs/build/phases.md for the phase output>
```

> Task breakdown is authored at the start of the phase, not up front, so it
> reflects what the preceding phases actually produced.

## Acceptance

- An address resolves to an area, which drives config resolution
