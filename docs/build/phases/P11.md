# P11 — Order

Depends on: P09, P10
Branch: `phase/11-order`

## Tasks

```
P11.T01  <written when this phase starts — see docs/build/phases.md for the phase output>
```

> Task breakdown is authored at the start of the phase, not up front, so it
> reflects what the preceding phases actually produced.

## Acceptance

- The lifecycle state machine rejects illegal transitions
- Placing an order twice with one idempotency key creates one order
