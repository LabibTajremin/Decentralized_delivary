# P13 — Payment

Depends on: P11
Branch: `phase/13-payment`

## Tasks

```
P13.T01  <written when this phase starts — see docs/build/phases.md for the phase output>
```

> Task breakdown is authored at the start of the phase, not up front, so it
> reflects what the preceding phases actually produced.

## Acceptance

- The module exposes only PaymentContract with transport-neutral DTOs
- COD ledger reconciles
- Webhooks are idempotent
