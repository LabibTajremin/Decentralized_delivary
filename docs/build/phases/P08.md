# P08 — Discovery

Depends on: P02, P03, P07
Branch: `phase/08-discovery`

## Tasks

```
P08.T01  <written when this phase starts — see docs/build/phases.md for the phase output>
```

> Task breakdown is authored at the start of the phase, not up front, so it
> reflects what the preceding phases actually produced.

## Acceptance

- A user sees merchants within the configured base radius
- A user with zero merchants in radius is offered expansion
- Expanding increases the quoted delivery fee
- Expansion stops at the division boundary and returns a clear terminal state
- Two users in different divisions never see each other's merchants
